# Atur semua file teks agar disimpan dengan LF di Git, tapi bisa dikonversi ke CRLF di Windows
* text=auto

# File shell script (biasanya untuk Linux/macOS) harus pakai LF
*.sh text eol=lf

# File batch script (Windows) pakai CRLF
*.bat text eol=crlf

# File Go dan konfigurasi pakai LF
*.go text eol=lf
go.mod text eol=lf
go.sum text eol=lf

# File Markdown dan workspace pakai LF
*.md text eol=lf
*.code-workspace text eol=lf

# Jangan ubah line ending untuk file biner
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary